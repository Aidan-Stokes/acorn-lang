Array :: struct {
    data: []int,
    length: int
}

len :: fn(arr: Array) -> int {
    return arr.length
}

push :: fn(arr: Array, value: int) {
}

pop :: fn(arr: Array) -> int {
    return 0
}

get :: fn(arr: Array, index: int) -> int {
    return 0
}

set :: fn(arr: Array, index: int, value: int) {
}

make :: fn(size: int) -> Array {
    arr: Array;
    return arr;
}
