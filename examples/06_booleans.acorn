import "core:fmt"

// Boolean literals
main :: fn() -> int {
    flag <- true
    
    if (flag) {
        fmt.print(1)  // prints 1
    }
    
    flag <- false
    if (!flag) {
        fmt.print(0)  // prints 0
    }
    
    return 0
}