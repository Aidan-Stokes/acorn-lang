import "core:fmt"

main :: fn() -> int {
    // Test 1: len on array literal - works!
    l1 <- len([1, 2, 3, 4, 5])
    fmt.println(l1)
    
    // Test 2: array indexing - works!
    arr <- [10, 20, 30]
    fmt.println(arr[1])
    
    return 0
}