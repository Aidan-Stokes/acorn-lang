import "core:fmt"

// For loops - range
main :: fn() -> int {
    // Range loop: 0 to 10 (inclusive)
    fmt.print("Range 0..3:")
    for i in 0..3 {
        fmt.print(i)  // prints 0, 1, 2, 3
    }
    
    return 0
}