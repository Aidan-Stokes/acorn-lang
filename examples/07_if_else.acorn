import "core:fmt"

// If/else statements
main :: fn() -> int {
    x <- 10
    if (x > 5) {
        fmt.print(1)  // prints 1
    } else {
        fmt.print(0)
    }

    if (x < 5) {
        fmt.print(0)
    } else {
        fmt.print(2)  // prints 2
    }

    return 0
}