import "core:fmt"

// For loops - infinite loop with break
main :: fn() -> int {
    n <- 0
    
    // Infinite loop
    for {
        n <- n + 1
        if (n >= 3) {
            break
        }
    }
    
    fmt.print(n)  // prints 3
    
    return 0
}