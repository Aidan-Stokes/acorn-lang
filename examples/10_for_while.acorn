import "core:fmt"

// For loops - while loop (condition)
main :: fn() -> int {
    n <- 0
    
    // While loop
    for n < 3 {
        fmt.print(n)  // prints 0, 1, 2
        n <- n + 1
    }
    
    return 0
}